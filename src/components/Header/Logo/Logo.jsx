import React from 'react'
import s from './Logo.module.css'

const Logo = (props) => {
    return (
        <div className = {s.logo}>
            My Travel App
        </div>
    )
}

export default Logo;